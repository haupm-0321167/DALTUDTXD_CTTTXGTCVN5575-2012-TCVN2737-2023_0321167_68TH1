<Window x:Class="DALTUDTXD_CTTTXGTCVN5575_2012_TCVN2737_2023_0321167_68TH1.Views.POPUP_PHAMHAU.Errorview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DALTUDTXD_CTTTXGTCVN5575_2012_TCVN2737_2023_0321167_68TH1.Views.POPUP_PHAMHAU"
        mc:Ignorable="d"
               Title="Errorview" Height="480" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <Style x:Key="ModernGroupCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0F172A" Opacity="0.04" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="SidebarLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#DC2626"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ErrorButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Value="#B91C1C" Property="Background"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Value="#CBD5E1" Property="Background"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid >
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/Images/background.png"
Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Border Grid.Row="0" Background="#2A4B3D" Height="55" Padding="20,0">
            <TextBlock Text="GỬI BÁO CÁO LỖI HỆ THỐNG (SYSTEM ERROR REPORT)"
                       Foreground="White"
                       FontSize="15"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </Border>


        <Border Grid.Row="1" Style="{StaticResource ModernGroupCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>


                <TextBlock Grid.Row="0" Grid.Column="0" Text="Phân hệ gặp lỗi:" Style="{StaticResource SidebarLabelStyle}" Margin="5,10"/>
                <ComboBox Grid.Row="0" Grid.Column="1" Margin="5,10" x:Name="cbbCategory" Height="35" VerticalContentAlignment="Center" Padding="10,5">
                    <ComboBoxItem Content="Tổ hợp &amp; Tính toán thiết kế" IsSelected="True"/>
                    <ComboBoxItem Content="Kết nối dữ liệu ETABS"/>
                    <ComboBoxItem Content="Xuất báo cáo Excel"/>
                    <ComboBoxItem Content="Đồ họa bản vẽ 2D / CAD"/>
                    <ComboBoxItem Content="Khác"/>
                </ComboBox>


                <TextBlock Grid.Row="1" Grid.Column="0" Text="Email liên hệ:" Style="{StaticResource SidebarLabelStyle}" Margin="5,10"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="5,10" x:Name="txtContactEmail"
                         Text="khachhang.hau@gmail.com"
                         Style="{StaticResource FormTextBoxStyle}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Thông báo hệ thống:" Style="{StaticResource SidebarLabelStyle}" Margin="5,10" VerticalAlignment="Top"/>
                <TextBox Grid.Row="2" Grid.Column="1" Margin="5,10" x:Name="txtErrorMessage"
                         Text="{Binding ErrorMessage, Mode=OneWay}"
                         Style="{StaticResource FormTextBoxStyle}" IsReadOnly="True" Background="#F1F5F9" Foreground="#64748B"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Mô tả chi tiết lỗi:" Style="{StaticResource SidebarLabelStyle}" Margin="5,10" VerticalAlignment="Top"/>
                <TextBox Grid.Row="3" Grid.Column="1" Margin="5,10" x:Name="txtUserDescription"
                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="100"
                         Text="Mô tả các bước thực hiện trước khi gặp lỗi..."
                         Padding="10" Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1"/>


                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5,20,5,5">
                    <Button Content="ĐÓNG" Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,10,0" Click="Close_Click"/>
                    <Button Content="GỬI BÁO CÁO" Style="{StaticResource ErrorButtonStyle}" Width="120" Click="Send_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
